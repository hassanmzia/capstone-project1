# =============================================================
# CNEAv5 Neural Interfacing Platform — Environment Configuration
# Copy this file to .env and fill in your values
# =============================================================

# Database (PostgreSQL + TimescaleDB + pgvector)
DB_PASSWORD=changeme
POSTGRES_PORT=5435
POSTGRES_DB=neural_interface
POSTGRES_USER=neural_admin

# Django
DJANGO_SECRET_KEY=changeme
DJANGO_DEBUG=false
DJANGO_ALLOWED_HOSTS=demo.eminencetechsolutions.com,108.48.39.238,172.168.1.95,localhost,127.0.0.1,django

# Redis
REDIS_PORT=6385

# Frontend (served via Nginx SSL proxy)
FRONTEND_PORT=3025
VITE_API_URL=/api
VITE_WS_URL=wss://demo.eminencetechsolutions.com:3025

# BFF
BFF_PORT=3026

# Agent Ports
AGENT_ACQUISITION_PORT=8088
AGENT_PROCESSING_PORT=8089
AGENT_HARDWARE_PORT=8090
AGENT_STORAGE_PORT=8091
AGENT_AIML_PORT=8092
AGENT_NOTIFICATION_PORT=8093
AGENT_LLM_PORT=8094

# MCP
MCP_SERVER_PORT=8087

# LLM (Ollama — existing on host)
OLLAMA_BASE_URL=http://172.168.1.95:12434
OLLAMA_CHAT_MODEL=deepseek-r1:7b
OLLAMA_EMBED_MODEL=nomic-embed-text

# Optional: Set API keys to enable cloud LLM providers in the model selector
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# LangFuse (LLM Observability)
LANGFUSE_PORT=8095
LANGFUSE_PUBLIC_KEY=changeme
LANGFUSE_SECRET_KEY=changeme
LANGFUSE_NEXTAUTH_SECRET=changeme
LANGFUSE_SALT=changeme

# Host IP (your server's local/private IP)
HOST_IP=172.168.1.95
